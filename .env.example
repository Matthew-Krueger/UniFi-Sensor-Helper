# Copy to .env and fill in real values. Never commit .env.
# See CLAUDE.md — the coding agent must never read the real .env file.

# No bootstrap admin credential here by design (SPEC.md section 3). The
# first account is created through the app itself: sign-up is open only
# while the users table is empty, and that first account is always
# superadmin. Once it exists, further accounts require being logged in.

# Used to sign session cookies. Generate a long random value, e.g.:
#   openssl rand -base64 48
SESSION_SECRET=changeme-generate-a-long-random-value

# HTTPS — self-signed cert/key paths (see DEPLOY.md for generation command).
# Never commit the actual cert/key files.
TLS_CERT_PATH=./certs/dev-cert.pem
TLS_KEY_PATH=./certs/dev-key.pem

PORT=8443

# Where the SQLite database file lives (sensors, latches, latch state, users,
# and Protect console connections). Consoles are added/edited through the
# UI (Settings page), not here — a site can have more than one console, so
# host/API key belong in the user-editable database, not a fixed env var.
# See API_NOTES.md for the API this connects to.
DATABASE_PATH=./data/app.db
